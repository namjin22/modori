# 작업 인수인계

## AI 교대 운영

이 프로젝트는 Claude Pro와 Codex를 사용 가능한 한도에 따라 교대한다. 다음 작업자는
`CLAUDE.md`의 **AI 교대 작업 규칙**을 먼저 읽고, 아래 문서와 `git status`를 확인한 뒤
작업을 이어간다. 한도 소진이나 외부 오류로 멈출 때는 이 파일에 마지막 상태와 다음
작업을 추가한다.

이번 비교에서는 `todomate.net`을 자동 브라우저에서 직접 열 수 없어 공식 앱 스토어의
최신 기능 설명을 보조 자료로 사용했다. 공식 설명에 있는 루틴, 색상 할 일, 타이머,
일기, 리마인더, AI, 친구 반응 중 현재 MVP에 없는 타이머·일기·리마인더·AI는
`docs/roadmap.md`의 영구 제외 규칙으로 유지한다.

사용자 확정 규칙: 타이머·일기·리마인더·AI는 v2가 아니라 영구 제외 기능이다. 사용자가
명시적으로 결정을 바꾸기 전에는 어떤 AI도 이를 구현하거나 작업 목록에 다시 넣지 않는다.

## 2026-09-19 — 연속 체크 진행률 보정

`TodoProgress`와 `useCompletionCount`를 추가해 체크박스의 낙관적 상태와 오늘 완료
개수를 같은 클라이언트 컨텍스트로 묶었다. 서버 응답 순서가 뒤섞여도 진행률이 먼저
맞고, 서버에서 다시 렌더링되면 `key`로 초기 DB 값에 동기화된다. `tests/e2e/todo.spec.ts`
에 두 할 일을 연속 완료하는 회귀 테스트를 추가했다.
타입 검사, ESLint, Vitest 53개, production build는 통과했다. 전용 E2E 재실행은
Neon DB 호스트에 연결할 수 없어 시작 단계에서 실패했으며, 코드 assertion 실패는
아니다.

## 2026-09-19 — 일정과 할 일 분리

### 완료한 범위

- `Event` 모델과 `Todo.color`를 추가했고, 마이그레이션
  `20260917121414_events_and_todo_color`를 적용했다. 기존 데이터는 수정하거나 삭제하지
  않는다.
- 오늘 화면에서 일정은 별도 섹션에서 생성·수정·삭제·되돌리기할 수 있다. 일정은 제목,
  시작일, 종료일, 색을 가지며 최대 366일 범위로 제한한다.
- 달력에는 일정 제목만 색 칩으로 표시한다. 할 일 제목은 달력에 표시하지 않으며, 완료한
  할 일의 색으로 날짜 칸 하단부터 채운다.
- 일정과 할 일은 여덟 색상 팔레트에서 고른다. 할 일 색을 고르지 않으면 카테고리 색을
  사용한다. 서버 액션은 팔레트 밖의 색상 값을 저장하지 않는다.
- 피드는 완료한 할 일만 유지하며 일정은 노출하지 않는다.

### 주요 파일

- 데이터: `prisma/schema.prisma`,
  `prisma/migrations/20260917121414_events_and_todo_color/migration.sql`
- 일정 서버 액션: `app/(tabs)/events/actions.ts`
- UI: `components/event-form.tsx`, `components/event-section.tsx`,
  `components/month-calendar.tsx`, `components/todo-row.tsx`
- 색상 규칙: `lib/colors.ts`, `components/color-swatches.tsx`
- 회귀 테스트: `tests/e2e/events.spec.ts`

### 검증 기록

- Prisma schema 검증 통과.
- `npm run verify` 통과: 단위 테스트(UTC, Asia/Seoul, America/New_York)와 Playwright
  E2E를 포함해 총 54개 통과. 실행 시간은 약 6.1분이었다.
- 이벤트 전용 E2E는 일정 생성, 여러 날 일정 표시, 삭제 후 되돌리기, 할 일 개별 색상
  저장과 달력 채우기를 검증한다.
- 기존 작업 로그에서 `prisma migrate deploy`의 적용 완료를 확인했다. 다만 2026-09-19에
  다시 실행한 `prisma migrate status`는 상세 원인 없이 Prisma schema engine 오류로
  종료됐다. 스키마 자체는 `prisma validate`를 통과했다.

### 다음 작업자 주의사항

- `.env`는 수정하지 않는다.
- 모든 날짜 연산은 `lib/date.ts`를 사용해 Asia/Seoul 기준을 지킨다.
- 일정은 친구 피드에 보이지 않는 것이 의도된 동작이다.
- `components/day-mark.tsx`는 달력의 클로버 표식을 색상 채우기로 교체하면서 삭제됐다.
- 기능 변경 뒤에는 `npm run verify`를 실행한다. Playwright 설정은 production build와
  단일 워커를 사용하므로 완료까지 수 분이 걸린다.

## 2026-09-20 — 다음 작업자 메모

현재 워크트리는 일정과 할 일 분리 기능을 포함한 미커밋 상태다. `next build`, TypeScript,
ESLint는 통과했다. 다만 현재 E2E를 다시 실행하면 다음 두 테스트가 실패한다.

- `tests/e2e/category-add.spec.ts`: 카테고리 +로 첫 번째 할 일을 저장한 뒤 입력칸이
  서버 액션 재렌더링으로 사라진다.
- `tests/e2e/todo.spec.ts`: 두 할 일을 빠르게 동시에 체크하면 진행률이 2/2로 올라가지
  않는다. 두 요청이 겹친 상태에서 클라이언트 낙관 상태와 서버 RSC 응답이 경쟁한다.

두 실패 모두 테스트를 고쳐 우회하지 않았다. 토글을 Route Handler로 분리하는 실험을 했지만
현재 안정적인 해결이 아니어서 적용하지 않고 원래 서버 액션 구조로 되돌렸다. 다음 작업은
카테고리 추가와 토글을 하나의 명시적인 클라이언트 mutation 흐름으로 정리한 뒤, 이 두
회귀를 먼저 고쳐야 한다. 이 두 테스트가 통과하기 전에는 UI/성능 개선 커밋을 만들거나
push하지 않는다.

추가 확인: `app/(tabs)/page.tsx`의 `TodoProgress` remount 키를 날짜만 보도록 바꾸면서
카테고리 + 연속 입력 회귀는 해결됐다. 공용 빠른 추가는 요청 완료 경계를 위해 클라이언트
요청과 `/api/todos`를 실험 중이며, 현재 남은 실패는 두 할 일을 동시에 체크하는 진행률
회귀 하나다. 네이티브 POST 뒤 늦은 hydration과 체크 이벤트가 겹치는 상황에서 재현되므로,
이 문제를 해결하기 전에는 해당 API 실험을 최종 기능으로 간주하지 않는다.

2026-09-20 추가 검증: 전체 `npm run verify`는 E2E 55/56, 시간대 단위 테스트 56/56 통과했다. 미래 루틴의 예정 완료
회귀는 서버 완료 개수 동기화 보정으로 해결되어 전용 테스트가 통과했다. health endpoint와
출시 전 체크리스트도 추가했다. 남은 것은
두 할 일을 `Promise.all`로 동시에 체크하는 회귀 1건이며, 단일 체크·달력 갱신·일정
관련 테스트는 통과한다.

2026-09-20 출시 점검 추가: 예정 루틴 완료 액션이 클라이언트에서 보낸 날짜와 규칙을
서버에서 재검증하도록 수정했다. 잘못된 날짜 조작 회귀 테스트가 추가됐고 루틴 E2E 6개가
통과한다. Todo API의 날짜 파싱 실패도 400으로 반환한다.

추가로 피드 cursor를 `doneAt + id` 복합 기준으로 수정했고, 캘린더 일정 제목을
`aria-describedby`로 스크린리더에 전달하도록 보강했다. 피드·캘린더·일정 관련 테스트가
통과한다.

색상 대비 계산기를 추가해 일정 칩의 텍스트 색을 배경 대비에 맞춰 선택하도록 보강했고,
유효한 팔레트·밝은 색·잘못된 색상 fallback 단위 테스트를 추가했다.

DataGSM OAuth Provider를 추가했다. PKCE S256, state, `datagsm:self_read`, token 교환,
userinfo 매핑, ACTIVE 상태 검증을 사용한다. `DATAGSM_CLIENT_ID`가 설정된 환경에서만
로그인 버튼이 노출된다. 실제 DataGSM 계정으로 Production redirect와 userinfo를 확인하는
수동 검증은 아직 남아 있다.

브랜드 시각 언어를 투두메이트의 클로버 계열과 분리하기 위해 도리를 펭귄 캐릭터로 교체하고,
대표 색상을 초록에서 파랑으로 변경했다. 기존 mood 이름과 반응 저장값은 호환성을 위해
유지했고 mascot E2E와 Production build를 통과했다.

## 2026-09-20 — 다음 에이전트 필수 인수인계

### DataGSM OAuth 현재 상태

- Production에서 Google 로그인은 정상이다.
- DataGSM 로그인 버튼도 Production에 노출된다.
- DataGSM 로그인 화면까지는 이동하지만, 아이디·비밀번호 입력 후
  `/api/auth/error?error=Configuration`으로 돌아온다.
- Vercel invocation에서 확인한 내용:
  - Route: `/api/auth/[...nextauth]`
  - External APIs: `No outgoing requests`
  - callback 이후 Auth.js Configuration 오류
  - 한 로그에서는 `/api/auth/callback/datagsm`의 `[auth] details`에
    `error: invalid_request`가 보였지만 전체 `error_description`은 아직 확보하지 못했다.
- Google/DataGSM 계정 중복 단계까지 도달하지 않은 것으로 보인다. userinfo 요청이 발생하지
  않았으므로 계정 linking 로직을 원인으로 단정하지 말 것.
- Production Client 설정 화면에서는 다음을 확인했다:
  - Client ID: `e461f60e-4a4b-4bb6-9d6a-dbf36abbaefa`
  - Production redirect: `https://modori.vercel.app/api/auth/callback/datagsm`
  - 권한 표시: `SELF_READ`
- DataGSM 문서 기준 endpoint:
  - authorize: `https://oauth.authorization.datagsm.kr/v1/oauth/authorize`
  - token: `https://oauth.authorization.datagsm.kr/v1/oauth/token`
  - userinfo: `https://oauth.resource.datagsm.kr/userinfo`
- 현재 `lib/auth.ts`는 다음 상태다:
  - `scope: "datagsm:self_read"`
  - `checks: ["pkce"]` (DataGSM callback에서 state가 누락될 가능성을 분리하기 위해 state를 임시 제거)
  - Auth.js 기본 token 요청을 `customFetch`로 가로채 JSON body로 변환
  - 기본 Authorization header 제거
  - Client Secret은 환경변수에서 읽음
  - ACTIVE 상태만 허용
  - `allowDangerousEmailAccountLinking: true`
- Auth.js v5 beta의 `token.request` override는 실제로 호출되지 않는 사례가 있어 사용하지 않는다.
- 다음 에이전트는 추측으로 scope/checks를 반복 변경하지 말고, Vercel callback 로그의 정확한
  `error_description` 또는 DataGSM 친구에게 callback 요청/응답 원문을 확인받아야 한다.

### DataGSM 다음 확인 순서

1. DataGSM 친구에게 해당 Client의 실제 허용 scope 문자열이 정확히 `datagsm:self_read`인지 확인한다.
2. authorize 성공 후 DataGSM이 redirect하는 실제 callback query에 `code`가 있는지, `state`가
   있는지 확인한다. Authorization Code와 토큰은 공유하지 않는다.
3. PKCE cookie가 Vercel callback에서 존재하는지 확인한다. 없으면 Auth.js cookie 설정과
   DataGSM redirect 흐름을 점검한다.
4. callback에서 token endpoint 요청이 실제로 나가는지 확인한다.
5. token 요청의 DataGSM 응답 status와 `error_description`만 로그로 확인한다.
6. 정상 userinfo JSON의 `status`, `email`, `student.name`을 확인한다.

### 캐릭터 현재 상태

- 사용자 피드백: 현재 펭귄/몽글 생명체 디자인은 “별로”이며 수용하지 않았다.
- 현재 `components/dori.tsx`는 파란 몽글 생명체 SVG지만 최종 디자인으로 간주하지 않는다.
- 다음 에이전트는 기존 SVG를 조금씩 고치는 대신 새 콘셉트부터 다시 설계해야 한다.
- 요구사항:
  - 투두메이트 클로버 캐릭터와 시각적으로 완전히 달라야 한다.
  - 작고 귀엽고 한눈에 기억되는 실루엣이어야 한다.
  - `happy`, `like`, `fire`, `clap`, `party`, `calm`, `sad`, `confused`, `hello` mood를 모두 표현해야 한다.
  - 반응 저장값 `👍🔥👏🎉`과 기존 컴포넌트 호출 API는 깨지지 않아야 한다.
  - 새 캐릭터 시안은 mascot E2E와 Production build를 통과시킨다.

### Git/배포 상태

- Production main 최신 merge commit: `ce48819` 또는 이후 DataGSM scope/캐릭터 관련 merge commit
- develop과 main은 squash merge 방식으로 동기화한다.
- PR check는 동시 체크 회귀와 CI Neon 데이터 경합 때문에 종종 실패한다. 실패 내용을 숨기고
  테스트를 바꾸지 않는다.
- Production migration 상태는 확인 완료:
  - `Database schema is up to date!`

## 2026-09-21 — DataGSM 토큰 형식과 캐릭터 재설계

### DataGSM: 확인한 사실

추측 대신 실제로 측정했다. 비밀값은 출력하지 않았다.

- **authorize 요청에 state가 없었다.** 로컬에서 `/api/auth/signin/datagsm`의 302
  Location을 찍어 확인했다. `checks: ["pkce"]`라서 파라미터 자체가 생성되지 않는다.
  나머지(response_type, scope, redirect_uri, code_challenge, S256)는 문서와 일치했다.
  `checks: ["state", "pkce"]`로 되돌린 뒤 state와 `authjs.state` 쿠키가 생기는 것을 확인했다.
- **PKCE 쿠키는 정상이었다.** authorize 직후 `authjs.pkce.code_verifier`가 저장된다.
- **토큰 엔드포인트는 JSON 본문 + 본문 자격 증명만 받는다.** 일부러 틀린 code로
  세 형식을 보내 응답을 비교했다.
  - JSON 본문 + client_id/client_secret → 400 `invalid_grant` (형식·인증 통과)
  - JSON 본문 + Basic 헤더 → 401 "잘못된 형식의 Authorization 헤더입니다"
  - form 본문 → 415 "지원되지 않는 미디어 타입입니다"
  기존 코드는 customFetch가 Authorization 헤더를 지우는데 본문에도 자격 증명이 없어서
  어떤 인증도 실리지 않았다. `client_secret_post`로 바꿔 본문에 들어가게 했다.
- **scope는 `datagsm:self_read`가 맞다.** `SELF_READ`로 보내면 400 `invalid_scope`다.
- authorize 엔드포인트는 state가 없어도 302를 준다. 즉 로그인 화면까지는 간다.
  로그인 이후 콜백에서 무엇이 돌아오는지는 실제 계정이 있어야 확인된다.

### DataGSM: 해결됨 (2026-09-21 확인)

프로덕션 배포 후 실제 DataGSM 로그인이 성공했다. 원인은 위 두 가지가 맞았다.
아래 "남은 확인"은 해결 전 절차이므로 참고용으로만 남긴다.

### DataGSM: (해결 전) 남은 확인

프로덕션에서 DataGSM 로그인을 한 번 더 시도한 뒤, Vercel 로그의 `[auth:error]` 한 줄이
필요하다. 이제 `providerError`와 `providerErrorDescription`을 함께 남긴다.
authorization code와 토큰은 로그에 남지 않는다.

- `providerError: invalid_request` 계열이면 DataGSM이 콜백에 오류를 실어 보낸 것이다.
- `name: InvalidCheck`면 PKCE/state 쿠키 문제다.
- `message`에 `unexpected "iss"`가 있으면 DataGSM이 RFC 9207 `iss`를 보내는 것이므로
  provider에 `issuer`를 정확한 문자열로 지정해야 한다. 값은 DataGSM 쪽에 확인해야 한다.

### 캐릭터

`components/dori.tsx`를 파란 고양이로 다시 그렸다. 귀로 실루엣을 잡아 24px에서도
구분되고, 눈 반지름 7과 선 굵기 3.2로 작은 크기에서 표정이 남는다. mood 아홉 가지와
`Dori({ mood, size, label, className })` 호출 방식, 반응 저장값(👍🔥👏🎉)은 그대로다.
장식으로 쓸 때는 `aria-hidden`을 유지한다.

### 되돌린 구현

빠른 추가가 `/api/todos`로 폼을 직접 POST해서 할 일 하나 넣을 때마다 전체 페이지가
다시 열렸다(목록이 사라졌다가 로딩 뼈대가 보였다). 서버 액션으로 되돌렸다.
완료 개수는 모듈 전역 Map·sessionStorage·타이머·`router.refresh` 대신 `useOptimistic`
하나로 바꿨다. `toggleTodo`에 빠져 있던 `revalidatePath("/")`도 되살렸다.
쓰지 않게 된 `app/api/todos` 라우트 두 개는 삭제했다.

### 검증 기록 (2026-09-21)

- `npx tsc --noEmit` 통과, `npm run lint` 통과, `npx next build` 통과.
- `npm run verify`: 단위 테스트 56개가 세 타임존에서 모두 통과.
  E2E는 55개 통과, 1개 실패(아래).

### 실패하는 테스트 1개 — 구현이 아니라 테스트 문제

`tests/e2e/todo.spec.ts:48` "할 일을 연달아 완료해도 완료 개수가 즉시 맞는다".

측정한 내용:

- 이 테스트는 `getByRole("button", { name: "완료", exact: true }).all()`로 체크박스
  목록을 미리 붙잡는다. 그런데 첫 번째를 누르는 순간 그 버튼의 이름이 "완료 취소"로
  바뀌어 조건에서 빠진다. 그래서 두 번째 클릭이 엉뚱한 자리를 가리키고,
  서버 액션 요청이 **한 건만** 나간다(요청 수를 세어 확인했다).
- 같은 동시 클릭을 로케이터로 그때그때 찾게 하면(`nth(0)`, `nth(1)`을 클릭 시점에 해석)
  요청 2건, 진행률 "2개 중 2개 완료", DB도 둘 다 `done=true`로 정상이다.
  간격 0ms와 120ms 모두 통과한다.
- 즉 동시 체크 동작 자체는 정상이고, 실패는 테스트가 붙잡아 둔 목록이 낡아서 생긴다.

사용자 승인을 받고 로케이터만 고쳤다. 확인하는 내용은 그대로다.
각 할 일 줄 안에서 체크박스를 찾고, 앞 클릭이 반환되면 바로 다음을 누른다.
서버 응답은 기다리지 않으므로 첫 저장이 끝나기 전에 두 번째를 누르는 상황은
그대로 검증된다.

두 클릭을 `Promise.all`로 같은 순간에 보내면 1ms 안에 겹칠 때 한쪽 DOM 이벤트가
사라진다. 브라우저에 `[dom-click]` 리스너를 붙여 계측했을 때, 겹치면 클릭이 한 번만
도달했고 두 번 도달한 경우에는 항상 요청 2건·진행률 2/2·DB 둘 다 완료로 정상이었다.

### 최종 검증 (2026-09-21)

`npm run verify` 전체 통과. 단위 56개가 세 타임존에서 통과, E2E 56개 통과.
`npx next build`도 통과했다.

## 2026-09-21 — UI/UX 다듬기 1차

화면을 전부 찍어 놓고(임시 Playwright 스펙, 커밋 전 삭제함) 눈에 걸리는 것부터 고쳤다.

- **기본 카테고리 색** (`app/onboarding/actions.ts`): 공부 `#2563eb`, 운동 `#3b82f6`이
  브랜드 파랑과 거의 같아서, 색으로 구분한다는 전제 자체가 무너져 있었다.
  공부 `#8b5cf6`, 운동 `#00b26a`, 생활 `#f59e0b`로 바꿨다.
  **이미 가입한 계정은 옛 색 그대로다.** 카테고리 관리에서 직접 바꿔야 한다.
- **카테고리 색 고르기** (`app/(tabs)/categories/page.tsx`, `components/color-swatches.tsx`):
  할 일·일정은 팔레트, 카테고리만 `<input type="color">`였다. 전부 팔레트로 통일했다.
  팔레트에 없는 옛 색은 "지금 쓰는 색" 견본으로 함께 보여준다. 안 그러면 아무것도
  고르지 않은 상태가 되어 저장이 조용히 무시된다.
- **로그인 실패 안내** (`lib/auth.ts`, `app/login/page.tsx`): `pages.error`를 `/login`으로
  돌리고 오류 코드를 한국어로 바꿔 `role="alert"`로 띄운다. 그전에는 Auth.js 기본
  영어 "Server error" 화면이었다(사용자가 DataGSM 실패 때 본 그 화면).
  `/login`은 로그인이 필요 없는 화면이라 되돌이 리디렉션이 생기지 않는다.
- **일정 줄** (`components/event-section.tsx`): 날짜 범위와 "수정"이 붙어 보여 `gap-3`으로 띄웠다.

### 검증

`npm run verify` 통과 (tsc + lint + E2E 56/56, 6.3분).

중간에 `todo.spec.ts:100`이 한 번 실패했다. 카테고리 수정 폼의 색 묶음 legend를
`${category.name} 색`으로 달았더니 목록의 카테고리 이름과 글이 겹쳐
`getByText("공부")`가 두 곳에 걸렸다. 테스트가 아니라 구현을 고쳤다 —
legend는 펼친 카테고리 안이므로 이름 없이 "색"이면 충분하다.

## 2026-09-21 — 말투·간격·기본 카테고리·프로필 사진

### 화면

- **포커스 테두리가 색 밑줄과 겹쳤다.** `app/globals.css`의 `:focus-visible`이 레이어
  밖에 있어서 Tailwind 유틸리티(`focus-visible:outline-none`)보다 항상 셌다.
  레이어 밖 규칙은 특정도와 관계없이 레이어 안 규칙을 이긴다. `@layer base`로 감쌌다.
- **간격**: 세로로 쌓이는 `flex-col gap-*`을 한 단계씩, 카드 안쪽 `p-3`→`p-4`,
  화면 위아래 `pt-6 pb-8`→`pt-8 pb-12`.
- **말투**: 단정하는 문장을 전부 `~해요`로. 길게 설명하던 문구도 한 줄로 줄였다.

### 기본 카테고리와 입력

- 기본 카테고리를 `Today's` 하나로 줄였다.
- 화면 맨 아래 공용 입력칸(`QuickAddForm`)을 지웠다. 이제 할 일을 적는 길은
  카테고리 칩의 `+` 하나뿐이다.
- 그 입력칸으로 할 일을 넣던 E2E 20개를 `tests/e2e/todo-helpers.ts`로 옮겼다.
- **보관한 카테고리에 `+`가 남아 있었다.** 예전에는 공용 폼의 카테고리 목록에서
  빠지는 것으로 막았는데, 그 폼이 사라지면서 막을 곳이 없어졌다.
  `groupByCategory`가 `archived`를 내려주고 `CategoryAdder`가 `+`를 숨긴다.

### 파비콘

`app/icon.svg`에 도리 얼굴을 그렸다. 16px까지 줄어들어서 몸은 빼고 얼굴만 담았고,
밝은 탭에서도 보이게 브랜드 파랑을 깔았다. 같은 그림으로 `public/icons`의 PNG
세 개를 다시 구웠다. 새 의존성 없이 Playwright로 그려서 저장했다.

### 프로필 사진

- `User.profileEmoji`를 지우고 `profileImage`(128×128 data URL)를 넣었다.
  **컬럼 삭제는 사용자 승인을 받았다.** 마이그레이션 `20260921120000_profile_image`.
  `prisma migrate dev`는 비대화형 환경에서 막히므로 `migrate diff`로 SQL을 뽑아
  폴더를 직접 만들고 `migrate deploy`로 적용했다.
- 사진은 브라우저에서 가운데를 정사각형으로 잘라 128×128 JPEG로 줄인 뒤
  data URL로 보낸다. 서버는 `lib/profile-image.ts`에서 형식과 길이를 다시 본다.
  폼 값을 그대로 믿으면 몇 MB짜리가 DB로 들어간다.
- 사진이 없으면 파비콘과 같은 도리 얼굴(`components/avatar.tsx`)을 쓴다.
- 프리셋 이모지 목록은 없앴다.

## 2026-09-21 — 수정 패널 정리와 파비콘 다시 그리기

### 할 일·일정 수정 패널

사용자 요청으로 **고치기와 지우기만 남겼다.**

- 할 일: 색 고르기, 카테고리 바꾸기, **내일로 넘기기**를 뺐다.
  `postponeTodo` 서버 액션과 그 테스트(`category-add.spec.ts`)도 함께 지웠다.
  되살리려면 그 커밋을 되돌리면 된다.
- 일정: 색 고르기를 뺐다. 색은 `DEFAULT_EVENT_COLOR`로 고정한다.
  달력에서는 일정 이름으로 알아본다.
- `updateTodo`가 폼에서 categoryId와 color를 읽어 덮어쓰고 있었다. 폼이 이제
  글자만 보내므로, 그대로 두면 저장할 때마다 카테고리가 날아간다. 글자만 고친다.
- `ColorSwatches`는 카테고리 관리 화면에서만 쓴다.

### 파비콘

전에는 64 좌표계에 얼굴을 새로 그렸더니 몸이 있는 도리와 달라 보여서 다른
캐릭터가 됐다. 이제 `components/dori.tsx`와 **같은 좌표의 도형을 그대로 쓰고**
`viewBox="20 3 80 80"`로 머리와 어깨만 잘라낸다. `components/avatar.tsx`의
`DoriFace`도 같은 그림이다. 둘을 고칠 때는 같이 고친다.

### 그 밖

- 로그인 버튼 두 개의 높이를 `h-14`로 맞췄다.
- 프로필 사진의 "도리로 되돌리기"를 "삭제"로 바꿨다.
- `dayFillStyle`의 완료 색이 진해서(`55`) 날짜 칸 안에 칩이 하나 더 있는 것처럼
  보였다. `3d`로 낮췄다.
- 프로필 화면 안내가 "친구가 내 하루를 열면 이렇게 보여요"인데 미리보기가 없어
  무엇을 가리키는지 알 수 없었다. "닉네임과 사진은 친구에게 보여요"로 바꿨다.

## 2026-09-22 — 떠 있는 창(모달)으로 바꾸기

### 왜

펼침(`<details>`)으로 열면 아래 내용이 밀려서 화면 비율이 흔들렸다. 할 일을
고치려고 누를 때마다 목록이 위아래로 움직여 어디를 보고 있었는지 잃는다.

### 무엇

`components/modal.tsx`를 만들었다. 브라우저가 가진 `<dialog>`를 쓴다.
포커스 가두기, Esc로 닫기, 뒤쪽 클릭 막기가 이미 들어 있다.

- **카테고리 칩**: 오른쪽 `+`를 없애고 이름 전체를 누르면 창이 뜬다.
- **일정**: "+ 일정 만들기" 버튼을 없애고 "일정" 제목을 누르면 만들기 창이 뜬다.
  일정 이름을 누르면 고치는 창이 뜬다.
- **할 일**: "수정" 글자를 없앴다. 할 일 글자를 누르면 창이 뜬다.
- 창 안에는 **입력칸 하나와 삭제뿐**이다. 저장 버튼은 없고 Enter로 저장한다.
  입력칸은 열릴 때 커서가 잡혀 있다.

### 걸린 것

- **Tailwind preflight가 `<dialog>`의 `margin: auto`를 지운다.** 그대로 두면 창이
  왼쪽 위에 붙는다. `m-auto`를 직접 준다.
- `::backdrop`은 유틸리티로 잘 닿지 않아서 `globals.css`에 직접 썼다.
- **닫힌 창의 안을 그려두면 안 된다.** 한 화면에 할 일이 여럿이면 같은 이름표가
  여러 개 남아 테스트가 어느 것인지 못 고르고, 다시 열어도 `autoFocus`가
  다시 걸리지 않는다. `open`일 때만 안을 그린다.

### 카테고리가 하나도 없을 때

카테고리 칩이 곧 할 일을 적는 자리라, 다 지우면 적을 곳이 사라진다. 루틴도
카테고리 안에 묶인다. 두 화면 모두 "카테고리를 먼저 만들어주세요"와 만들기
링크를 띄운다. 홈에서는 이때 빈 화면 안내를 겹쳐 띄우지 않는다.

## 2026-09-22 — 하단바 흔들림과 검증 속도

### 하단바

화면마다 하단 탭의 비율이 달라 보였다. 크기를 직접 재 보니 헤드리스에서는
다섯 화면 모두 똑같았다. 원인은 **스크롤바**였다. 피드만 내용이 길어 스크롤바가
생기고 다른 화면은 안 생긴다. 윈도우 크롬은 스크롤바가 자리를 차지하므로,
그때마다 화면 폭이 그만큼 줄어 가운데 정렬된 하단 탭과 내용이 좌우로 밀린다.
`html { scrollbar-gutter: stable }`로 자리를 늘 비워 둔다.

### 검증 13.3분 → 3.1분

E2E를 `workers: 1`로 묶어 둔 이유는 계정 이름이 파일마다 고정이라 파일끼리
서로의 계정을 지웠기 때문이다. 이름에 실행 꼬리표를 붙여 그 문제를 없애고
파일끼리 병렬로 돌린다(로컬 4, CI 2). `fullyParallel: false`라 한 파일 안의
순서는 그대로 지켜진다.

### CI가 main에서만 실패하던 이유

`feed.spec.ts`의 팔로우 테스트가 CI에서 세 번 연달아 깨졌다. 로컬과 PR 검사는
통과했다. 원인은 코드가 아니라 **계정 이름이 실행마다 같다는 것**이었다.
`testInfo.testId`는 실행이 달라도 같은 값이라, 로컬 검증과 CI가 같은 Neon
데이터베이스에서 같은 계정을 만들고 지운다. CI가 팔로우를 누르는 사이 로컬이
그 계정을 지우면 `followUser`가 대상을 못 찾고 조용히 끝나, 버튼이 "팔로우 중"으로
바뀌지 않는다. `tests/e2e/run-tag.ts`의 꼬리표로 실행끼리 겹치지 않게 했다.

## 2026-09-23 — 기록(통계) 화면

투두메이트 앱스토어 설명과 우리 화면을 다시 비교했다. 타이머·일기·AI·위젯은
영구 제외 항목이라 빼면, 남는 공백은 **통계**였다. 우리는 "이번 달 완료 N개"가
전부라 어느 카테고리를 잘 지키는지도, 며칠째 이어왔는지도 알 수 없었다.

`/stats`를 만들었다. 달마다 끝낸 개수·해낸 비율·이어온 날, 카테고리별 막대,
요일별 막대를 보여준다.

- 세는 규칙은 `lib/stats.ts`에 순수 함수로 뒀다. DB도 화면도 필요 없어서
  단위 테스트로 경계를 잡는다(`tests/unit/stats.test.ts`).
- **이어온 날은 오늘이 비어 있으면 어제부터 센다.** 그러지 않으면 자정을 넘기는
  순간 쌓아온 날이 0으로 보인다.
- 이어온 날은 달 경계를 넘으므로 1년치를 따로 읽는다. 달 통계만 읽으면
  1일에 항상 0이나 1이 된다.
- 이번 달에서는 "다음 달"을 감춘다. 아직 오지 않은 달에는 아무것도 없다.

### CI 워커

`workers: 2`로 올렸더니 CI에서 네 개가 타임아웃으로 깨졌다. 러너가 작아 Next
서버와 CPU를 뺏는다. CI는 1로 되돌리고 개발 PC만 4로 둔다(13분 → 3분).

## 2026-09-23 — 속도 측정과 D-day

### 속도: 앱이 아니라 거리 문제다

정적 파일(`/icon.svg`)과 서버 컴포넌트(`/login`)의 TTFB를 나란히 쟀다.
둘 다 0.4~0.7초로 같다. 연결 수립만 0.12~0.15초다. 즉 **서버가 그리는 시간은
거의 없고, 남은 건 한국과 싱가포르 사이 왕복**이다. 앱 코드로 줄일 게 없다.

번들도 확인했다. `/login`이 받는 JS는 압축 후 185KB이고, dnd-kit은 거기에
들어 있지 않다(청크를 받아 `DndContext` 등을 찾아봤다). 코드 쪼개기는 이미 된다.

정말 줄이려면 DB를 서울 리전으로 새로 만들고 함수도 icn1로 보내는 수밖에 없다.
이건 데이터 이전이라 베타 전에 따로 판단한다.

### D-day

투두메이트 앱스토어 리뷰에서 가장 많이 요청되는 기능이다. 우리도 같은 구멍이
있었다. 일정에 날짜가 있는데 그 날짜를 열어보기 전에는 며칠 남았는지 몰랐다.

- `lib/dday.ts` — 시작 전이면 `D-n`, 시작한 날이면 `D-DAY`, 여러 날짜리의
  가운데면 "진행 중", 끝난 일정이면 null. 순수 함수라 단위 테스트로 잡는다.
- **끝난 일정에는 붙이지 않는다.** D+를 붙이면 지난 기록을 볼 때마다 화면이
  숫자로 뒤덮인다.
- 오늘 화면에 아직 오지 않은 일정 세 개를 같이 띄운다. 그러지 않으면 D-day를
  만들어 놓고도 그 날짜로 옮겨가야만 보인다.

## 2026-09-23 — 다시 자리에서 적기, M 로고, 친구 화면

사용자 요청으로 여러 가지를 되돌리거나 바꿨다.

- **추가는 다시 자리에서.** 카테고리 이름을 누르면 칩 아래에, "일정"을 누르면
  그 아래에 입력칸이 열린다. 고치기는 날짜까지 손봐야 해서 창으로 남긴다.
  "일정" 옆 `+`도 없앴다. 대신 일정이 하나도 없을 때만 점선 줄로 자리를 만든다.
  제목만 있으면 누를 수 있는 곳인지 모른다.
- **루틴 만들기**의 큰 파란 동그라미와 넓은 카드를 없앴다. 한 줄짜리 제목이
  아래 목록보다 무거워 보였다. 일정과 같은 모양으로 맞췄다.
- **친구 화면을 내 오늘 화면과 같은 구성으로** 바꿨다. 큰 빈 카드 하나 대신
  카테고리 칩으로 묶는다. 보이는 범위는 그대로다(팔로우한 사람의, 완료한,
  공개 카테고리 할 일). 그래서 일정은 보여주지 않는다.
- **친구 찾기에서 이름을 누르면** 그 사람 화면으로 간다. 팔로우한 사람만
  열 수 있으므로 팔로우 전에는 링크로 만들지 않는다.
- **달력과 주간 줄의 완료 색 채우기를 없앴다.** `lib/colors.ts`의 `dayFillStyle`도
  같이 지웠다. 달력에는 일정 이름만 남는다. 그 달에 얼마나 했는지는 기록 화면에서 본다.
- **로고.** `components/logo.tsx`에 M 한 글자 로고를 그렸다. 글꼴이 아니라 선이라
  글꼴 없는 환경에서도 그려지고 16px까지 줄여도 읽힌다. 파비콘(`app/icon.svg`),
  홈 화면 아이콘, 로그인 화면 가운데가 모두 같은 그림이다.
  기본 프로필 사진은 그대로 도리 얼굴이다.

## 2026-09-23 — 반응 열둘과 도리 다듬기

### 반응을 이모지로 되돌리고 열둘로 늘렸다

도리의 표정으로 그렸더니 24px로 줄어든 얼굴 넷이 서로 구별되지 않았다.
열둘로 늘리면 더 그렇다. 이모지는 설명 없이도 뜻이 통하므로 이모지 그대로 쓴다.
도리는 크게 나오는 자리(빈 화면, 축하, 오류, 없는 주소)에서만 쓴다.

- 저장값은 그대로 이모지 문자다. 앞의 넷(`👍🔥👏🎉`)은 순서와 글자를 건드리지
  않았고 새 여덟 개를 뒤에 붙였다. 마이그레이션이 필요 없다.
- **아무도 누르지 않은 반응은 줄에 넣지 않는다.** 열두 개를 늘 늘어놓으면
  할 일 한 줄보다 반응 줄이 길어진다. 새로 보낼 때는 `♡`를 눌러 창에서 고른다.
- `lookOfReaction`(도리 표정을 돌려주던 함수)을 `labelOfReaction`으로 바꿨다.

### 도리

- **눈에 반짝임을 넣었다.** 검은 타원만 있으면 인형 눈처럼 멍해 보인다.
  이게 인상을 가장 크게 바꿨다.
- 꼬리를 붙였다. 옆선이 밋밋하면 어느 쪽이 앞인지 덜 읽힌다.
- 배를 아주 옅게 밝혔다. 진하게 하면 몸이 흰 덩어리로 뭉쳐 발이 사라진다
  (0.5로 해보고 0.28로 낮췄다).

## 2026-09-23 — 팔레트 여덟 색, 진행 막대 고정, 친구 화면 달력

### 팔레트

빨주노초파보 + 흰색 + 검정, 무지개 순서로 여덟 개만 둔다. 색이 많으면 무엇을
골랐는지 기억하지 못하고 비슷한 색끼리 구별도 안 된다.

**흰색과 검정이 들어오면서 색을 글씨에 쓰던 곳이 전부 깨진다.** 흰 글씨는
라이트 모드에서, 검정 글씨는 다크 모드에서 배경과 같아진다. 그래서
카테고리 칩은 색을 **배경으로** 쓰고 글씨는 `contrastTextColor`로 고른다.
색 견본과 색 점에는 얇은 테두리를 둘렀고, 완료 체크의 ✓도 대비로 고른다.

`DEFAULT_EVENT_COLOR`가 `PALETTE[1]`을 가리키고 있었다. 팔레트가 바뀌면 같이
바뀌므로 파랑을 직접 적었다. 새 가입자의 `Today's`도 파랑이다.

### 진행 막대

할 일이 없어도 `0개 중 0개 완료`로 자리를 지킨다. 첫 할 일을 적는 순간 막대가
생기면 화면이 한 번 밀리고, 무엇이 늘었는지도 알아채기 어렵다.
"할 일" 제목은 지웠다. 바로 아래가 할 일 목록이라 말할 필요가 없다.

### 문구

`{오늘|이 날} 할 일을 다 끝냈어요` → `할 일을 다 끝냈어요`.
친구 화면의 `이 날 끝낸 할 일이 없어요`도 `끝낸 할 일이 없어요`로 줄였다.
날짜는 바로 위에 이미 적혀 있다.

### 친구 화면

내 오늘 화면과 같은 구성으로 맞췄다. 넓은 화면에서는 왼쪽 프로필·달력,
오른쪽 목록. 좁은 화면에서는 주간 줄과 "달력" 버튼. 날짜 화살표도 넣었다.
`PageFrame`이 `/`만 넓게 쓰고 있어서 `/feed/u/`도 넓히도록 고쳤다.

### 도리

360px로 띄워 놓고 어디가 어색한지 찾았다.

- **꼬리를 뺐다.** 굵기를 줄이고 위치를 옮겨도 계속 앞발로 보였다.
- **어깨를 좁혔다.** 머리와 몸통이 같은 폭이라 목이 없고 한 덩어리로 보였다.
- 볼터치가 얼굴 절반을 덮고 있어 줄였다.
- 파티 고깔이 귀 바깥에 떠 있어 머리 위로 옮겼다.

## 2026-09-24 — 파비콘이 한 번도 바뀌지 않았던 이유

`app/favicon.ico`에 create-next-app이 넣어둔 기본 아이콘(Vercel 삼각형)이 남아
있었다. Next는 이 파일로 `<link rel="icon" href="/favicon.ico">`를 내보내고,
브라우저는 그걸 쓴다. 그래서 `app/icon.svg`를 몇 번 바꿔도 탭 아이콘은 그대로였다.
프로덕션 `<head>`를 받아서 확인했다.

`favicon.ico`를 새 로고로 다시 만들었다. ICO는 PNG를 그대로 담을 수 있어서
16·32·48·256 크기를 Playwright로 굽고 파이썬으로 묶었다(새 의존성 없음).

### 로고

M 한 글자 로고를 버리고 **고양이 귀가 달린 체크박스**로 바꿨다. 할 일을 체크하는
서비스라는 것과 캐릭터 도리를 한 도형에 담는다. 16px에서도 귀와 체크가 읽힌다.
`components/logo.tsx`, `app/icon.svg`, `app/favicon.ico`, `public/icons/*.png`가
모두 같은 그림이다. 홈 화면 아이콘은 투명하면 iOS가 검은 배경을 깔아서 흰 바탕에 앉혔다.

## 2026-09-24 — 흰색 견본 테두리, 뒤로가기, 팔 달린 도리

### 흰색 견본이 라이트 모드에서 안 보였다

앞서 테두리를 넣었다고 했는데, 팔레트 견본이 아니라 "지금 쓰는 색" 견본에만
넣었다. 게다가 라이트 모드 테두리 색(`#e8ebed`)은 흰 배경에서 거의 안 보인다.

`globals.css`에 `.color-edge`를 만들었다. 글자색을 22% 섞은 안쪽 선이라
라이트에서는 어두운 선, 다크에서는 밝은 선이 알아서 생긴다. 색 견본, 색 점,
카테고리 칩, 완료 체크, 일정·루틴 색 점에 모두 쓴다.

### 뒤로가기

글자 `←` 하나라 누를 수 있는 면적이 글자 크기뿐이었고, 흐린 색이라 버튼으로
보이지도 않았다. `components/back-link.tsx`에 40px 동그라미 + 꺾쇠 아이콘을
만들고 아홉 화면을 모두 바꿨다. 왼쪽 여백만큼 당겨서 아이콘이 본문 선에 맞는다.

### 도리: 팔을 달고 2등신으로

머리가 몸의 대부분이라 1.5등신에 가까웠고 팔이 없었다.

- 머리를 0.84배로 줄여 위로 올렸다. 얼굴 좌표를 전부 다시 적는 대신 **머리 묶음째
  변환**한다(`HEAD`). 줄인 만큼 테두리가 가늘어지므로 머리에는 선을
  `OUTLINE / HEAD_SCALE`로 준다.
- 몸통을 넓고 둥글게, 발을 그 아래에. 머리 하나에 몸 하나, 두 등신 남짓이다.
- **팔은 몸 뒤에 그린다.** 어깨가 옆구리에 숨어 몸에서 뻗어 나온 것처럼 보인다.
  위에 얹으면 막대가 몸에 붙어 있는 것처럼 보인다.
- 파티 고깔은 머리를 따라가야 해서 얼굴(머리 좌표)로 옮겼다.

### 로그인 로고

104px에서 64px로 줄였다. 버튼 두 개 위에 로고만 크게 떠 있었다.

### 테스트

`profile.spec.ts`가 고정 닉네임(`처음닉네임`)을 쓰고 있었다. 이메일에는 실행
꼬리표가 붙어 매번 바뀌므로, 중간에 끊긴 실행이 남긴 옛 계정은 지워지지 않고
닉네임만 붙잡고 있다. 닉네임에도 꼬리표를 붙였다.

## 2026-09-24 밤 — 자율 개선 루프 (사용자 요청: 10회)

사용자가 자는 동안 찾기 → 고치기 → 검증 → 재확인 → 배포를 열 번 돈다.
루프마다 여기에 무엇을 찾았고 무엇을 고쳤는지 남긴다.

### 루프 1 — 서버 액션 보안·권한

여덟 개 액션 파일을 전부 읽었다. 남의 데이터를 건드리는 구멍은 없었다(모든
갱신이 `userId`로 걸러진다). 대신 아래를 찾아 고쳤다.

- **보관한 카테고리에 새 할 일이 들어갈 수 있었다.** 화면은 막지만 `addTodo`는
  `archivedAt`을 보지 않았다. 다른 탭에서 보관하기 전에 열어 둔 입력칸이 남아
  있으면 들어간다. `archivedAt: null`을 조건에 넣었다.
- **반응을 같은 순간 두 번 누르면 오류 화면이 떴다.** 둘 다 "없으니 만든다"로
  가서 unique 제약(P2002)에 걸린다. 그 오류는 결과가 같으므로 넘긴다.
- **잘못된 날짜 문자열 하나가 오류 화면을 띄웠다.** `parseKSTDate`는 예외를
  던지는데 `addTodo`·`restoreTodo`·`completeScheduledRoutine`·`reorderTodos`가
  그대로 불렀다. `readDay`로 감싸 null이면 조용히 돌아간다.
- **카테고리 창이 저장하지 않고도 "저장했어요"라고 했다.** 이름을 비운 채 색을
  바꾸면 서버는 조용히 돌아가는데 화면은 성공이라고 했다. `updateCategory`가
  결과를 돌려주고 화면은 "이름을 적어주세요"를 빨갛게 띄운다. E2E로 고정했다.
- **카테고리 색이 팔레트 밖 값도 받았다.** 정규식만 보고 아무 16진 색이나
  받았다. 만들 때는 팔레트만, 고칠 때는 팔레트이거나 원래 쓰던 색만 받는다.

검증: `npm run verify` 통과 — 단위 70×3, E2E 62/62(카테고리 창 테스트 2개 추가).

### 루프 2 — 모바일 라이트 전 화면

두 계정에 카테고리·할 일·일정·루틴·팔로우·반응을 채워 넣고 19개 화면을 찍어
한 장씩 봤다(점검 스크립트는 저장소에 넣지 않고 scratchpad에서 돌린다).

- **(회귀 버그) 고른 색에 선택 링이 안 보였다.** 흰색 테두리용 `.color-edge`가
  `box-shadow`를 썼는데 Tailwind `ring`도 `box-shadow`다. 레이어 밖 규칙이라 항상
  이겨서 링을 지웠다. `border`로 바꾸고 `@layer components` 안에 넣었다.
- **진한 색 위 글씨가 탁했다.** `contrastTextColor`는 대비가 큰 쪽을 고르는데,
  파랑(#3b82f6)은 검정 쪽이 조금 더 커서 파란 칩에 검정 글씨가 올라갔다.
  굵은 글씨·아이콘용 `onColorText`를 만들어 흰 글씨와 3:1 이상이면 흰색을 쓴다.
  빨강·파랑·보라·검정 → 흰 글씨, 주황·노랑·초록·흰색 → 어두운 글씨.
- **소셜 피드가 할 일마다 큰 카드였다.** 이름과 날짜가 줄마다 반복되고 네 개면
  화면이 찼다. 붙어 있는 같은 사람·같은 날끼리 카드 한 장에 한 줄씩 묶는다.
  시간 순서는 그대로 지킨다.
- **"루틴 만들기"가 제목처럼 보였다.** 브랜드 색과 펼침 꺾쇠를 달았다.
- 설정의 "프로필 수정 →"만 옛 화살표라 꺾쇠로 맞췄다.
- 기록 화면의 흰색 카테고리 막대가 옅은 바탕에 묻혀 가장자리를 둘렀다.

테스트: 설정의 프로필 링크에 꺾쇠 svg가 생겨서, "링크 안의 svg"로 도리 얼굴을
찾던 `profile.spec.ts`가 두 개를 잡았다. 확인하는 내용은 그대로 두고 도리 얼굴의
viewBox로 좁혔다.

검증: `npm run verify` 통과 — 단위 70×3, E2E 62/62.

### 루프 3 — 다크 모드 전 화면

같은 데이터로 19개 화면을 다크 모드로 찍었다. 대부분 괜찮았다. 검정 카테고리
칩과 체크도 가장자리 선 덕분에 배경과 구분된다.

- **(버그) 기록 화면에서 0%인 카테고리 막대에 흰 점이 찍혔다.** 가장자리 선은
  폭이 0인 막대에도 그려진다. 0%면 막대를 그리지 않는다.
- **친구 화면에서 "달력" 버튼 하나가 한 줄을 통째로 차지했다.** 이름 옆으로 올렸다.
  "오늘로 돌아가기"는 오늘이 아닐 때만 날짜 아래에 붙는다.

검증: `npm run verify` 통과 — 단위 70×3, E2E 62/62.
